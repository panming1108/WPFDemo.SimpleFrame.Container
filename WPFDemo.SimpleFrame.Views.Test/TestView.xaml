<UserControl x:Class="WPFDemo.SimpleFrame.Views.Test.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFDemo.SimpleFrame.Views.Test"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WPFDemo.SimpleFrame.Infra.ControlsThemes;component/Fonts/FontStyle.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Button Width="100"
                    Height="30"
                    Command="{Binding TestViewClickCommand}">
                <TextBlock Style="{StaticResource FIcon}" Foreground="Black" Text="{StaticResource fa-500px}"/>
            </Button>
            <TextBlock Text="{Binding TestViewText}"
                       HorizontalAlignment="Center"
                       FontSize="16"/>
        </StackPanel>
    </Grid>
</UserControl>
